import {
    add,
    sub
} from './utils/math'
import {
    time,
    price
} from './utils/format'

console.log(add(1,5));
console.log(sub(6,5));

console.log(time.format())
console.log(price.format())